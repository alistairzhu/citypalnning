
## Installation steps:
* for Install & run TestRPC: $ npm install -g ethereumjs-testrpc
* $ testrpc
* open testrpc or geth console in one window
* in new window for Install Truffle: $ npm install –g truffle
* $ mkdir TestProject
* $ cd TestProject
* truffle unbox webpack
* clone or download Github
* unzip the downloade github and copy all folder files include contracts folder , app folder to TestProject and overwrite
* cd contracts
* truffle compile
* truffle migrate
* npm run dev
* open localhost:8080 base on npm run dev
* npm run build
